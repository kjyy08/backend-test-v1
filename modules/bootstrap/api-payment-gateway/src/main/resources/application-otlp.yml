management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  prometheus:
    metrics:
      export:
        enabled: true
  otlp:
    metrics:
      export:
        enabled: false

otel:
  service:
    name: ${spring.application.name}
  exporter:
    otlp:
      endpoint: ${OTEL_EXPORTER_ENDPOINT:http://localhost:4317}
      compression: gzip
      timeout: 10s
  traces:
    exporter: ${OTEL_EXPORTER_ENDPOINT:none}
    sampler: parentbased_traceidratio
    sampler.arg: 1
  metrics:
    exporter: ${OTEL_EXPORTER_ENDPOINT:none}
  logs:
    exporter: ${OTEL_EXPORTER_ENDPOINT:none}
  instrumentation:
    micrometer:
      enabled: true